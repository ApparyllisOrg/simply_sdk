name: simply_sdk
description: SDK for Apparyllis API
version: 1.0.11
author:

publish_to: none

environment:
  sdk: ">=2.14.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.2
  path_provider: ^2.0.1
  web_socket_channel: ^2.1.0
  uuid: ^3.0.4
  mongo_dart: ^0.7.0+2
  universal_html: ^2.0.8
  cloud_firestore: ^1.0.1 
  shared_preferences: ^2.0.8
  source_gen: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter: